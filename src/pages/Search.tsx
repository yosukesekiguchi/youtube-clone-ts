import React, { FC } from 'react';
import Layout from '../components/Layout';

const Search: FC = () => {
  return (
    <Layout>
      <div>Search</div>
    </Layout>
  );
};

export default Search;
